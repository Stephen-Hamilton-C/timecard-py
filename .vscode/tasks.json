{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Timecard as Standalone executable",
            "type": "shell",
            "command": "python3 -m nuitka --onefile timecard.py -o dist/timecard",
            "windows": {
                "command": "python -m nuitka --onefile timecard.py -o dist/timecard.exe"
            }
        }
    ]
}